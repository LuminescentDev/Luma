parent=keycloak.v2

# Keep the parent keycloak.v2 stylesheet (tooltips, social icons, layout helpers)
# and layer the Luma identity on top. luma.css must load last so its overrides win.
styles=css/styles.css css/luma.css
